# VLC → MAL Sync v0.3
MAL OAuth PKCE, MAL anime search, match selection, episode-progress update, and filename parsing.
Configure a MAL API application's Client ID in MainActivity.kt and redirect URI `malvlcsync://oauth`.
Cloud build is provided by `.github/workflows/build-apk.yml`.
